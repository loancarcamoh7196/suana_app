<%# Page info %>
	<div class="page-top-info">
		<div class="container">
			<h3>Detalle - <%= @product.title %></h3>
			<div class="site-pagination">
        <%= link_to 'Administración'.html_safe, '#' %>
				 /
				<%= link_to 'Productos'.html_safe , '#' %>
        /
				<%= link_to "Detalle - #{@product.title}".html_safe , '#' %>
			</div>
		</div>
	</div>
<%# Page info end %>
<%# Products- Show (Agregar detalle a Products) %>
<%# Ver: vendedor - admin %>
<div class="container">
  <div class="row">
    <div class="col-12 mt-4">
      <%# Detalles de Producto %>
      <%= render 'details' %>
    </div>
  </div>
</div>
<br>
<div class="container">
  <div class="row">
    <div class="col-12">
      <%= render 'table-details' %>
    </div>
  </div>
</div>
<br>
<div class="container">
  <div class="row">
    <div class="col-12">
      <%= render 'form-details' %>
    </div>
  </div>
</div>
<%= render 'modal' %>