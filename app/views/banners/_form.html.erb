<%= form_with(model: banner) do |form| %>
  <% if banner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(banner.errors.count, "error") %> prohibited this banner from being saved:</h2>

      <ul>
      <% banner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title, 'Título' %>
    <%= form.text_field :title, class: 'form-control', required: true %>
  </div>

  <div class="form-group">
    <%= form.label :description, 'Descripción' %>
    <%= form.text_area :description, class: 'form-control', required: true  %>
  </div>
  <div class="form-group">
    <%= form.label :image, 'Imagen' %>
    <%= form.file_field :image, class: 'form-control-file', required: true  %>
  </div>
  <div class="form-group">
    <%= form.label :actived, class: 'form-check-label' %>
    <%= form.check_box :actived, class: 'checkbox' %>
  </div>

  <div class="form-group">
    <%= form.submit %>
  </div>
<% end %>
